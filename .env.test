# TEST credentials — placeholders only. NOT real secrets.
# Comms stays in safe STUB mode (COMMS_LIVE=0): outbound SMS returns a fake sid,
# nothing is actually sent or charged. Flip COMMS_LIVE=1 and drop in real keys to go live.
PEPTIDENET_DEBUG=1
PEPTIDENET_SECRET_KEY=test-only-not-secret
PEPTIDENET_COMMS_LIVE=0
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx   # placeholder
TWILIO_AUTH_TOKEN=test_placeholder_token                # placeholder
TWILIO_DEFAULT_FROM=+15875550000
OPENAI_API_KEY=                                          # set for Whisper transcription
ELEVENLABS_API_KEY=                                      # set for TTS greetings
ANTHROPIC_API_KEY=                                       # set for AI-drafted replies

# Admin login for the control panel (created by the walkthrough):
#   user: admin   password: Test1234!
